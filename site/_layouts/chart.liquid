<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ page.title }}</title>
        {% include "header.html" %}
    </head>
    <body>
    <div>
      <h2><a href="/">{{ site.title }}</a></h2>
    
      <div>{{ page.description }}</div>
      {{ page.content }}
    </div>
  </body>
</html>
